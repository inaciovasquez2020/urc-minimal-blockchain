Example: Minimal Blockchain

System
Ledger with deterministic next block function.

Invariant
Each block extends chain uniquely; forks resolved by total order.

Interpretation
Deterministic minimal blockchain without probabilistic fork choice.
